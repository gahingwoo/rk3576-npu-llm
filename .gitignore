# --- Build output -----------------------------------------------------------
/br-out/
/br-dl/
*.log

# --- Installer offline cache (pre-downloaded in phase 1; deployment clone only)
/cache/

# --- Fetched at build time (GPL source pulled, not redistributed here) -------
/driver/rknpu/

# --- Closed / third-party runtimes and blobs (fetched, not redistributed) ----
/buildroot/dl/

# --- Model weights (large, licensed separately) ------------------------------
/model/
*.rkllm

# --- Kernel out-of-tree module build artifacts -------------------------------
*.o
*.ko
*.mod
*.mod.c
*.mod.o
*.order
Module.symvers
modules.order
.*.cmd
.tmp_versions/
.module-common.o

# --- Local bring-up logs / scratch -------------------------------------------
/dirty/

# --- Work-in-progress notes (drafts; will be organised before publishing) ----

# --- OS junk -----------------------------------------------------------------
.DS_Store
/kernel-dual/
